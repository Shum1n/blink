
<!-- <view class="container">
    <text>No.</text>
    <text>{{_index}}</text>
    <text>|</text>
    <text>{{month}}</text>
    <text>{{year}}</text>
</view> -->

<!-- view.container>((view.indexx-container>(text.plain{No.}+text.index{{{_index}}}+view.line))+view.data-container>(text.month{{{month}}}+text.year{{{year}}})) -->
<view class="container">
    <view class="index-container">
        <text class="plain">No.</text>
        <text class="index">{{_index}}</text>
        <view class="line"></view>
    </view>
    <view class="data-container">
        <text class="month">{{month}}</text>
        <text class="year">{{year}}</text>
    </view>
</view>